// import { useQuery } from '@tanstack/react-query';
// import vaultApi from 'src/apis/vault.api';
// import { VaultInfo, VaultInfoInContract } from 'src/types/vault.type';
// import { ContractClient } from 'src/utils/ContractClient';

// export const useUserBalance = ({ token, address }: { token: string, address: string }) => {
//     const { data: vaultInfos, isLoading } = useQuery({
//         queryKey: ['vault-list'],
//         queryFn: () => getVaultInfos(user),
//     });

//     return {
//         vaultInfos,
//         isLoading,
//         vaultDetail,
//         isLoadingDetail,
//     };
// };

// export const getUserBalance = 
